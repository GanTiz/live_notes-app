# Dependances specifiques au packaging desktop, en plus de requirements.txt.
# Separees du requirements.txt applicatif pour ne pas coupler l'usage serveur
# local (pip install -r requirements.txt && python app.py) au build desktop.
# pywebview : fenetre native sur macOS uniquement (WKWebView). Sur Windows,
# packaging/launcher.py lance Edge directement (subprocess + registre), sans
# dependance supplementaire — voir le commentaire en tete de launcher.py.
pywebview>=5,<6
pyinstaller>=6,<7
